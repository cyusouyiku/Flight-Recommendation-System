<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.app.mapper.AirportMapper">

    <resultMap id="BaseResultMap" type="com.example.app.entity.Airport">
        <id column="id" property="id"/>
        <result column="code" property="code"/>
        <result column="name" property="name"/>
        <result column="city" property="city"/>
        <result column="country" property="country"/>
        <result column="timezone" property="timezone"/>
    </resultMap>

    <select id="selectById" resultMap="BaseResultMap">
        SELECT id, code, name, city, country, timezone
        FROM airport
        WHERE id = #{id}
    </select>

    <select id="selectByCode" resultMap="BaseResultMap">
        SELECT id, code, name, city, country, timezone
        FROM airport
        WHERE code = #{code}
    </select>

    <select id="selectAll" resultMap="BaseResultMap">
        SELECT id, code, name, city, country, timezone
        FROM airport
    </select>

    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO airport (code, name, city, country, timezone)
        VALUES (#{code}, #{name}, #{city}, #{country}, #{timezone})
    </insert>

    <update id="update">
        UPDATE airport
        SET name = #{name}, city = #{city}, country = #{country}, timezone = #{timezone}
        WHERE id = #{id}
    </update>

    <delete id="deleteById">
        DELETE FROM airport WHERE id = #{id}
    </delete>

    <delete id="deleteByCode">
        DELETE FROM airport WHERE code = #{code}
    </delete>
</mapper>
